// This component is kept for backward compatibility but login popup has been removed
// Login is now handled via a separate Login page
import React from "react";

const Popup = () => {
  return null;
};

export default Popup;
